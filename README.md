# shadow_decoder
> Problem Statement:
> Build a C CLI tool named shadow_decode that reads a dynamically sized Base64-encoded string from stdin, decodes it entirely in RAM to reconstruct a valid .bmp file, and extracts a hidden text payload at a specific byte offset.

